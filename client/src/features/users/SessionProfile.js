import React from "react";

const SessionProfile = () => {
  return (
    <div>
      <p>
        Apply to join a session. Your application will need to be approved by
        the club administrator to ensure you are eligible to play.
      </p>
    </div>
  );
};

export default SessionProfile;
